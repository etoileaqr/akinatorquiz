// import 'package:akinatorquiz/model/typo_corrector.dart';

class Shared {
  static final Shared info = Shared._createInstance();
  Shared._createInstance();

  // TypoCorrector corrector = TypoCorrector();
}
